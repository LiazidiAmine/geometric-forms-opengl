libg3dx une sur couche d'opengl

cd ../m1
sh ./install.sh -> verifier ensuite le ~/bashrc
cd code3d
-------------------------

#include<g3x>
//variable globales
void init(void){
	//statique 1 fois
	//alloc meme
}

void quit(void){
	//1 fois atexit
}

void anim(void){//moteur 
	//que du calcul
}

void draw(void){//moteur de rendu
	//que du dessin
	//appelé dans boucle
	// pas dallocation memoire ici
	//ici opengl
	glPushMatrix(->pour appliquer le translate au cube seulement, ensuite on pop la matrice
	gltranslatef(0.5,0.5,0.5)->cree une transation et sapplique sur tout ce qui est derriere
	g3xMateriel(
	glxtSolidSphere(
	glPopMatrix(
	g3xMat(
	glutSolidCube
}

int main(void){
	g3x.InitWindow(argv,500,500);
	
	

	g3x.SetInitFunction(init);
	Exit (quit);
	Draw (draw);
	Anim (anim);//si pas d'animation par exemple, on met null a la place de anim	
return g3x.MainStart();
}

g3x_Materiel(g3xr, 0.5,0.5,0.5,0.5) on utilise des float. les params correspondent a r,g,b,a
